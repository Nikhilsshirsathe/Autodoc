"""
Backend models — we use supabase-py for all DB access.
SQLAlchemy ORM models are only needed if Alembic migrations are used.
Pydantic schemas (app/schemas/) are used for request/response validation.
"""
